====================================
VM DEPLOYMENT STEPS
====================================

Server: 20.195.127.226
User: azureuser
Password: bMe2@6XgqvYNV8$wzQh7fL#s

STEP 1: SSH into the server
----------------------------
ssh azureuser@20.195.127.226

STEP 2: Navigate to project directory
--------------------------------------
cd ~/CSEDU-Digital-Knowledge-Platform-main

STEP 3: Run the deployment script
----------------------------------
chmod +x quick-update.sh
./quick-update.sh

This script will:
- Pull latest code from GitHub
- Fix file structure issues
- Kill any processes blocking ports (5432, 6379, 8080, 8001, 9000, 9001, 3000, 80)
- Stop all containers
- Clean up orphaned containers
- Remove Docker cache
- Start services in order: postgres → redis → minio → api → rag → workers → frontend → nginx
- Check service health

STEP 4: Index library catalog books for RAG
--------------------------------------------
After services are running, index the catalog books.
This runs INSIDE the RAG container and connects to other services via Docker network:

docker compose exec rag python scripts/index_catalog_books.py

This creates embeddings for all library books so the RAG assistant can search them.
The script automatically connects to:
- Database: postgres:5432 (Docker service name)
- RAG API: localhost:8001 (same container)

STEP 5: Verify deployment
--------------------------
After the script completes, check services:

Frontend: http://20.195.127.226:3000
API: http://20.195.127.226:8080/health
RAG: http://20.195.127.226:8001/health

If you see 502 Bad Gateway, run diagnostics:
./check-services.sh

Common issues:
- API container not running: Check logs with docker compose logs api
- Port conflicts: Ports 8080, 3000, 8001 must be free
- Build failures: Check docker compose logs for errors

STEP 6: Check logs if needed
-----------------------------
docker compose logs -f frontend
docker compose logs -f api
docker compose logs -f rag

Check specific container status:
docker compose ps
docker compose logs --tail=100 api

====================================
WHAT WAS FIXED
====================================

1. Profile editing and password change dialogs now work
2. Files copied to correct location for Docker build
3. Port conflict resolution in deployment script
4. Proper service startup order
5. Enhanced RAG assistant with:
   - Intent detection (search/question/compare/summarize/availability)
   - Context-aware conversations
   - Confidence scoring
   - Suggested follow-up questions (bilingual)
   - Conversation history support
6. **RAG NOW SEARCHES ALL 4 RESOURCE TYPES:**
   - ✓ Library Catalog (books)
   - ✓ Research Papers
   - ✓ Projects
   - ✓ Archives

====================================
NEW FEATURES AVAILABLE
====================================

1. Edit Profile: Click "Edit Profile" button on profile page
2. Change Password: Click "Change Password" button on profile page
3. **Comprehensive CSEDU Assistant:** Now searches across:
   - Library catalog books (title, author, ISBN, availability)
   - Research papers (full text)
   - Student projects (full text)
   - Archives (full text)
4. Search: Full-text search across catalog (fixed TypeScript errors)
5. Email notifications: Infrastructure ready (disabled by default)

====================================
IMPORTANT: RAG INDEXING
====================================

The RAG assistant now retrieves from ALL resources, but library catalog
books need to be indexed first. After deployment, run:

  docker compose exec rag python scripts/index_catalog_books.py

This runs inside the RAG container and automatically connects to the
database via Docker networking (postgres:5432).

This only needs to be run once (or when new books are added to catalog).

====================================
